# AGENTS.md

## Cursor Cloud specific instructions

This repository (`adaptive-productive`) is currently empty — it contains only a `README.md`. There are no application services, dependencies, build scripts, or tests to run.

**When code is added to this repository**, future agents should:
- Re-evaluate the tech stack and update this section with actual build/run/test/lint commands.
- Update the VM environment update script via `SetupVmEnvironment` to install the correct dependencies.

**Current state (as of initial setup):**
- No services to start.
- No linting, testing, or build commands available.
- No dependency installation required.
