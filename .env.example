# Adaptive Productive - Environment
PORT=3000
# DATABASE_URL=./data/app.db   # SQLite (default for local)
# DATABASE_URL=postgresql://... # PostgreSQL for production

# Google Gemini API key for AI features (study tips, task parsing, decomposition)
# Get one at https://aistudio.google.com/apikey
# GEMINI_API_KEY=your_key_here
